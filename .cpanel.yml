deployment:
  tasks:
    - export PATH=$HOME/bin:$HOME/.local/bin:$PATH
    - cd $HOME/repositories/toparriendos-react
    - git pull origin main
    - npm install
    - npm run build
    - rsync -av --delete build/ $HOME/public_html/
    - cp build/favicon.ico $HOME/public_html/favicon.ico
